# ContextOS — Render / retrieval-only API dependencies
# Install: pip install -r requirements-api.txt
#
# Does NOT include Docling, RapidOCR, OpenCV, PyMuPDF, torchvision, etc.
# Local PDF ingestion still uses requirements.txt (full stack).
#
# NOTE: sentence-transformers + torch are REQUIRED for query embeddings
# so they stay compatible with stored all-mpnet-base-v2 vectors (768-d).
# Cross-encoder reranker is NOT included (eval/local only).

fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3
python-multipart==0.0.32

supabase==2.31.0
httpx==0.28.1

groq==1.7.0

# Query embeddings (same model as indexed chunks)
sentence-transformers==6.0.1
torch==2.14.0
transformers==5.16.1
numpy==2.5.2
